{% extends 'base.html.twig' %}

{% block title %}Steps{% endblock %}

{% block body %}
    <h1>Steps</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ step.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ step.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_steps_index') }}">back to list</a>

    <a href="{{ path('app_backoffice_steps_edit', {'id': step.id}) }}">edit</a>

    {{ include('backoffice/steps/_delete_form.html.twig') }}
{% endblock %}
